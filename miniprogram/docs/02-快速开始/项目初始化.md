# 项目初始化指南

**文档版本**: v1.0.0 | **创建时间**: 2025-11-24 | **维护者**: 叨叨房车技术团队

> 🎯 **目标**: 理解叨叨房车小程序项目结构，掌握核心配置文件

---

## 📁 项目结构详解

### 核心目录结构

```
miniprogram/
├── pages/                    # 页面目录 (33个页面)
│   ├── home/                # 首页模块
│   │   └── index.vue        # 首页
│   ├── community/           # 社区模块
│   ├── crowdfunding/        # 众筹模块
│   ├── vehicles/            # 房车模块
│   ├── order/              # 订单模块
│   ├── payment/            # 支付模块
│   ├── profile/            # 个人中心
│   └── ...                 # 其他页面
├── components/              # 组件目录
│   ├── base/               # 基础组件
│   ├── business/           # 业务组件
│   ├── form/               # 表单组件
│   └── content/            # 内容组件
├── api/                    # 接口目录 (11个文件)
│   ├── auth.js            # 认证接口
│   ├── order.js           # 订单接口
│   ├── vehicle.js         # 房车接口
│   └── ...               # 其他接口
├── stores/                # 状态管理
│   ├── user.js           # 用户状态
│   └── vehicle.js        # 房车状态
├── utils/                 # 工具函数
│   └── request.js        # 网络请求封装
├── static/               # 静态资源
│   ├── images/          # 图片资源
│   └── icons/           # 图标资源
├── styles/              # 样式文件
│   ├── variables.scss   # 样式变量
���   ├── common.scss      # 通用样式
│   └── index.scss       # 样式入口
├── App.vue             # 应用入口
├── main.ts            # 主入口文件
├── manifest.json      # 应用配置
├── pages.json         # 页面配置
└── tsconfig.json      # TypeScript配置
```

### 核心文件说明

#### 1. 应用入口文件

**main.ts** - 应用启动入口
```typescript
import { createSSRApp } from 'vue'
import App from './App.vue'
import { createPinia } from 'pinia'

export function createApp() {
  const app = createSSRApp(App)

  // 状态管理
  const pinia = createPinia()
  app.use(pinia)

  return {
    app,
    pinia
  }
}
```

**App.vue** - 应用根组件
```vue
<script setup lang="ts">
import { onLaunch, onShow, onHide } from '@dcloudio/uni-app'
import { useUserStore } from '@/stores/user'

const userStore = useUserStore()

onLaunch(() => {
  console.log('App Launch')
  // 应用启动时检查登录状态
  userStore.checkLoginStatus()
})

onShow(() => {
  console.log('App Show')
})

onHide(() => {
  console.log('App Hide')
})
</script>

<style lang="scss">
@import '@/styles/index.scss';
</style>
```

#### 2. 核心配置文件

**manifest.json** - 应用配置
```json
{
  "name": "叨叨房车",
  "appid": "__UNI__AA65A95",
  "description": "房车租赁小程序",
  "versionName": "1.0.0",
  "versionCode": "100",
  "transformPx": false,
  "mp-weixin": {
    "appid": "你的微信小程序AppID",
    "setting": {
      "urlCheck": false,
      "es6": true,
      "enhance": true
    }
  }
}
```

**pages.json** - 页面路由配置
```json
{
  "pages": [
    {
      "path": "pages/home/index",
      "style": {
        "navigationBarTitleText": "首页"
      }
    },
    {
      "path": "pages/community/index",
      "style": {
        "navigationBarTitleText": "社区"
      }
    }
  ],
  "globalStyle": {
    "navigationBarTextStyle": "black",
    "navigationBarTitleText": "叨叨房车",
    "navigationBarBackgroundColor": "#ffffff",
    "backgroundColor": "#F8F8F8"
  },
  "tabBar": {
    "color": "#7A7E83",
    "selectedColor": "#FF9F29",
    "backgroundColor": "#ffffff",
    "list": [
      {
        "pagePath": "pages/home/index",
        "text": "首页",
        "iconPath": "static/tabbar/home.png",
        "selectedIconPath": "static/tabbar/home-active.png"
      }
    ]
  },
  "easycom": {
    "autoscan": true,
    "custom": {
      "^uni-(.*)": "@dcloudio/uni-ui/lib/uni-$1/uni-$1.vue"
    }
  }
}
```

---

## 🔧 初始化配置

### 1. TypeScript 配置

**tsconfig.json** - TypeScript配置
```json
{
  "compilerOptions": {
    "target": "es2017",
    "module": "es2015",
    "strict": true,
    "jsx": "preserve",
    "importHelpers": true,
    "moduleResolution": "node",
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "sourceMap": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
      "@components/*": ["./components/*"],
      "@api/*": ["./api/*"],
      "@utils/*": ["./utils/*"],
      "@stores/*": ["./stores/*"]
    },
    "lib": ["es2015", "dom", "dom.iterable"]
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "src/**/*.vue",
    "pages/**/*.vue",
    "components/**/*.vue"
  ],
  "exclude": ["node_modules"]
}
```

### 2. 样式系统初始化

**styles/variables.scss** - 样式变量
```scss
// 主色调
$primary-color: #FF9F29;
$secondary-color: #8860D0;

// 文字颜色
$text-primary: #333333;
$text-secondary: #666666;
$text-placeholder: #CCCCCC;

// 背景色
$bg-page: #F8F8F8;
$bg-card: #FFFFFF;

// 间距
$spacing-xs: 8rpx;
$spacing-sm: 16rpx;
$spacing-md: 24rpx;
$spacing-lg: 32rpx;

// 圆角
$border-radius-sm: 8rpx;
$border-radius-md: 16rpx;
$border-radius-lg: 24rpx;
```

**styles/index.scss** - 样式入口
```scss
@import './variables.scss';
@import './common.scss';

// 全局样式
page {
  background-color: $bg-page;
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', Helvetica, sans-serif;
}

// 通用容器
.container {
  padding: $spacing-md;
}

// 卡片样式
.card {
  background-color: $bg-card;
  border-radius: $border-radius-md;
  padding: $spacing-md;
  margin-bottom: $spacing-md;
}
```

### 3. 状态管理初始化

**stores/index.ts** - 状态管理入口
```typescript
import { createPinia } from 'pinia'
import { createPersistedState } from 'pinia-plugin-persistedstate'

const pinia = createPinia()

// 持久化插件
pinia.use(createPersistedState({
  storage: {
    getItem: (key) => uni.getStorageSync(key),
    setItem: (key, value) => uni.setStorageSync(key, value)
  }
}))

export default pinia
```

---

## 🚀 初始化验证

### 1. 创建测试页面

**pages/test/index.vue**
```vue
<template>
  <view class="test-page">
    <view class="header">
      <text class="title">初始化测试页面</text>
    </view>

    <view class="content">
      <uni-card title="项目配置测试">
        <view class="test-item">
          <text>Vue 3: ✅</text>
        </view>
        <view class="test-item">
          <text>TypeScript: ✅</text>
        </view>
        <view class="test-item">
          <text>uni-ui: ✅</text>
        </view>
        <view class="test-item">
          <text>Pinia: ✅</text>
        </view>
      </uni-card>

      <uni-card title="样式系统测试">
        <view class="test-primary">主色调测试</view>
        <view class="test-secondary">辅助色测试</view>
      </uni-card>

      <button @click="testApi" type="primary">API测试</button>
    </view>
  </view>
</template>

<script setup lang="ts">
import { ref } from 'vue'

const testApi = () => {
  uni.showToast({
    title: 'API测试成功',
    icon: 'success'
  })
}
</script>

<style lang="scss" scoped>
.test-page {
  padding: 30rpx;
}

.header {
  text-align: center;
  margin-bottom: 40rpx;

  .title {
    font-size: 40rpx;
    font-weight: bold;
    color: $text-primary;
  }
}

.content {
  .test-item {
    margin: 10rpx 0;
    font-size: 28rpx;
  }

  .test-primary {
    background-color: $primary-color;
    color: white;
    padding: 20rpx;
    border-radius: $border-radius-md;
    margin: 10rpx 0;
  }

  .test-secondary {
    background-color: $secondary-color;
    color: white;
    padding: 20rpx;
    border-radius: $border-radius-md;
    margin: 10rpx 0;
  }
}
</style>
```

### 2. 配置测试页面路由

在 **pages.json** 中添加测试页面
```json
{
  "pages": [
    // ... 其他页面
    {
      "path": "pages/test/index",
      "style": {
        "navigationBarTitleText": "初始化测试"
      }
    }
  ]
}
```

---

## 🔍 常见问题

### 问题1: 路径别名 @ 不生效
```json
// 解决方案: 在 manifest.json 中配置
{
  "srcDir": "src",
  "typeCheck": true
}
```

### 问题2: 样式变量未定义
```scss
// 解决方案: 检查样式导入顺序
// App.vue 中确保正确导入
@import '@/styles/index.scss';
```

### 问题3: TypeScript 类型错误
```bash
# 解决方案: 安装类型定义
npm install --save-dev @types/wechat-miniprogram
npm install --save-dev @dcloudio/types
```

### 问题4: Pinia 持久化不工作
```typescript
// 解决方案: 检查持久化插件配置
import { createPersistedState } from 'pinia-plugin-persistedstate'
```

---

## ✅ 初始化检查清单

### 项目结构检查
- [ ] 目录结构完整
- [ ] 核心文件存在
- [ ] 配置文件格式正确
- [ ] 静态资源路径正确

### 技术栈检查
- [ ] Vue 3 正常工作
- [ ] TypeScript 编译正常
- [ ] uni-ui 组件可使用
- [ ] Pinia 状态管理正常
- [ ] SCSS 样式编译正常

### 配置检查
- [ ] manifest.json 配置正确
- [ ] pages.json 路由配置正确
- [ ] tsconfig.json TypeScript配置正确
- [ ] 样式变量系统工作正常

### 功能检查
- [ ] 页面可以正常跳转
- [ ] 组件可以正常导入
- [ ] 接口可以正常调用
- [ ] 状态管理数据持久化
- [ ] 样式正确显示

---

## 🎯 下一步

初始化完成后，建议继续学习：

1. **[第一个页面开发](./第一个页面.md)** - 实践开发流程
2. **[样式系统文档](../03-样式系统/样式规范.md)** - 掌握样式开发
3. **[组件开发指南](../04-组件开发/组件概览.md)** - 学习组件开发
4. **[uni-app开发指南](../03-开发指南/uni-app开发指导文档.md)** - 深入框架使用

---

**文档维护**: 叨叨房车技术团队
**最后更新**: 2025-11-24
**版本**: v1.0.0
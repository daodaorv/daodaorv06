# 第一个页面开发

**文档版本**: v1.0.0 | **创建时间**: 2025-11-24 | **维护者**: 叨叨房车技术团队

> 🚀 **目标**: 15分钟内完成第一个功能页面开发，掌握小程序页面开发流程

---

## 🎯 开发目标

我们将开发一个**房车预订查询页面**，包含以下功能：
- 城市选择器
- 日期时间选择器
- 门店选择器
- 异地还车选项
- 查询按钮

最终效果参考小程序首页的房车预订模块。

---

## 📱 页面开发步骤

### 步骤1: 创建页面文件

**1.1 创建页面目录和文件**
```
在 pages/ 目录下创建 booking 目录
├── pages/
│   └── booking/
│       └── index.vue
```

**1.2 配置页面路由**
在 **pages.json** 中添加页面配置：
```json
{
  "pages": [
    // ... 其他页面
    {
      "path": "pages/booking/index",
      "style": {
        "navigationBarTitleText": "房车预订",
        "navigationBarBackgroundColor": "#FFFFFF",
        "backgroundColor": "#F8F8F8"
      }
    }
  ]
}
```

### 步骤2: 开发页面结构

**pages/booking/index.vue** - 基础模板
```vue
<template>
  <view class="booking-page">
    <!-- 页面标题 -->
    <view class="page-header">
      <text class="page-title">房车预订</text>
      <text class="page-subtitle">选择取车信息，查找可用房车</text>
    </view>

    <!-- 预订表单 -->
    <view class="booking-form">
      <!-- 取车城市 -->
      <view class="form-item">
        <view class="form-label">取车城市</view>
        <picker
          mode="selector"
          :range="cityList"
          :value="selectedCityIndex"
          @change="onCityChange"
        >
          <view class="picker-content">
            <text class="picker-text">{{ selectedCity || '请选择取车城市' }}</text>
            <uni-icons type="right" size="20" color="#999"></uni-icons>
          </view>
        </picker>
      </view>

      <!-- 取车门店 -->
      <view class="form-item">
        <view class="form-label">取车门店</view>
        <picker
          mode="selector"
          :range="storeList"
          :value="selectedStoreIndex"
          @change="onStoreChange"
          :disabled="!selectedCity"
        >
          <view class="picker-content" :class="{ disabled: !selectedCity }">
            <text class="picker-text">{{ selectedStore || '请先选择城市' }}</text>
            <uni-icons type="right" size="20" color="#999"></uni-icons>
          </view>
        </picker>
      </view>

      <!-- 取车时间 -->
      <view class="form-item">
        <view class="form-label">取车时间</view>
        <picker
          mode="datetime"
          :value="pickupTime"
          @change="onPickupTimeChange"
        >
          <view class="picker-content">
            <text class="picker-text">{{ formatDateTime(pickupTime) || '请选择取车时间' }}</text>
            <uni-icons type="right" size="20" color="#999"></uni-icons>
          </view>
        </picker>
      </view>

      <!-- 还车时间 -->
      <view class="form-item">
        <view class="form-label">还车时间</view>
        <picker
          mode="datetime"
          :value="returnTime"
          @change="onReturnTimeChange"
        >
          <view class="picker-content">
            <text class="picker-text">{{ formatDateTime(returnTime) || '请选择还车时间' }}</text>
            <uni-icons type="right" size="20" color="#999"></uni-icons>
          </view>
        </picker>
      </view>

      <!-- 异地还车 -->
      <view class="form-item">
        <view class="form-label">
          <text>异地还车</text>
          <switch
            :checked="isDifferentReturn"
            @change="onDifferentReturnChange"
            color="#FF9F29"
          />
        </view>
      </view>

      <!-- 还车城市和门店(异地还车时显示) -->
      <template v-if="isDifferentReturn">
        <view class="form-item">
          <view class="form-label">还车城市</view>
          <picker
            mode="selector"
            :range="cityList"
            :value="selectedReturnCityIndex"
            @change="onReturnCityChange"
          >
            <view class="picker-content">
              <text class="picker-text">{{ selectedReturnCity || '请选择还车城市' }}</text>
              <uni-icons type="right" size="20" color="#999"></uni-icons>
            </view>
          </picker>
        </view>

        <view class="form-item">
          <view class="form-label">还车门店</view>
          <picker
            mode="selector"
            :range="returnStoreList"
            :value="selectedReturnStoreIndex"
            @change="onReturnStoreChange"
            :disabled="!selectedReturnCity"
          >
            <view class="picker-content" :class="{ disabled: !selectedReturnCity }">
              <text class="picker-text">{{ selectedReturnStore || '请先选择城市' }}</text>
              <uni-icons type="right" size="20" color="#999"></uni-icons>
            </view>
          </picker>
        </view>
      </template>
    </view>

    <!-- 查询按钮 -->
    <view class="search-button-container">
      <button
        class="search-button"
        :class="{ disabled: !canSearch }"
        :disabled="!canSearch"
        @click="onSearch"
      >
        查询可用房车
      </button>
    </view>
  </view>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'

// 响应式数据
const cityList = ref<string[]>(['北京', '上海', '广州', '深圳', '成都', '杭州'])
const storeList = ref<string[]>([])
const returnStoreList = ref<string[]>([])

const selectedCityIndex = ref<number>(-1)
const selectedCity = ref<string>('')
const selectedStoreIndex = ref<number>(-1)
const selectedStore = ref<string>('')

const selectedReturnCityIndex = ref<number>(-1)
const selectedReturnCity = ref<string>('')
const selectedReturnStoreIndex = ref<number>(-1)
const selectedReturnStore = ref<string>('')

const pickupTime = ref<string>('')
const returnTime = ref<string>('')
const isDifferentReturn = ref<boolean>(false)

// 计算属性
const canSearch = computed(() => {
  return selectedCity.value &&
         selectedStore.value &&
         pickupTime.value &&
         returnTime.value &&
         (!isDifferentReturn.value || (selectedReturnCity.value && selectedReturnStore.value))
})

// 方法
const onCityChange = (e: any) => {
  const index = e.detail.value
  selectedCityIndex.value = index
  selectedCity.value = cityList.value[index]

  // 重置门店选择
  selectedStoreIndex.value = -1
  selectedStore.value = ''
  loadStores(selectedCity.value)
}

const onStoreChange = (e: any) => {
  const index = e.detail.value
  selectedStoreIndex.value = index
  selectedStore.value = storeList.value[index]
}

const onReturnCityChange = (e: any) => {
  const index = e.detail.value
  selectedReturnCityIndex.value = index
  selectedReturnCity.value = cityList.value[index]

  // 重置还车门店选择
  selectedReturnStoreIndex.value = -1
  selectedReturnStore.value = ''
  loadReturnStores(selectedReturnCity.value)
}

const onReturnStoreChange = (e: any) => {
  const index = e.detail.value
  selectedReturnStoreIndex.value = index
  selectedReturnStore.value = returnStoreList.value[index]
}

const onPickupTimeChange = (e: any) => {
  pickupTime.value = e.detail.value
}

const onReturnTimeChange = (e: any) => {
  returnTime.value = e.detail.value
}

const onDifferentReturnChange = (e: any) => {
  isDifferentReturn.value = e.detail.value

  // 如果关闭异地还车，清空还车相关信息
  if (!isDifferentReturn.value) {
    selectedReturnCityIndex.value = -1
    selectedReturnCity.value = ''
    selectedReturnStoreIndex.value = -1
    selectedReturnStore.value = ''
  }
}

const loadStores = (city: string) => {
  // 模拟加载门店数据
  const mockStores: Record<string, string[]> = {
    '北京': ['北京朝阳门店', '北京海淀门店', '北京丰台门店'],
    '上海': ['上海浦东门店', '上海静安门店', '上海徐汇门店'],
    '广州': ['广州天河门店', '广州越秀门店', '广州海珠门店'],
    '深圳': ['深圳南山门店', '深圳福田门店', '深圳罗湖门店'],
    '成都': ['成都锦江门店', '成都武侯门店', '成都青羊门店'],
    '杭州': ['杭州西湖门店', '杭州上城门店', '杭州滨江门店']
  }

  storeList.value = mockStores[city] || []
}

const loadReturnStores = (city: string) => {
  // 加载还车门店数据（与取车门店相同）
  loadStores(city)
  returnStoreList.value = storeList.value
}

const formatDateTime = (dateTime: string) => {
  if (!dateTime) return ''
  const date = new Date(dateTime)
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')} ${String(date.getHours()).padStart(2, '0')}:${String(date.getMinutes()).padStart(2, '0')}`
}

const onSearch = () => {
  if (!canSearch.value) {
    uni.showToast({
      title: '请完善查询条件',
      icon: 'none'
    })
    return
  }

  const searchData = {
    pickupCity: selectedCity.value,
    pickupStore: selectedStore.value,
    pickupTime: pickupTime.value,
    returnTime: returnTime.value,
    isDifferentReturn: isDifferentReturn.value,
    returnCity: isDifferentReturn.value ? selectedReturnCity.value : '',
    returnStore: isDifferentReturn.value ? selectedReturnStore.value : ''
  }

  console.log('查询参数:', searchData)

  // 跳转到车辆列表页面
  uni.navigateTo({
    url: '/pages/vehicles/index?' + new URLSearchParams(searchData as any).toString()
  })
}

// 生命周期
onMounted(() => {
  // 设置默认时间为当前时间
  const now = new Date()
  pickupTime.value = formatDateTime(now.toISOString())

  // 设置默认还车时间为第二天
  const tomorrow = new Date(now.getTime() + 24 * 60 * 60 * 1000)
  returnTime.value = formatDateTime(tomorrow.toISOString())
})
</script>

<style lang="scss" scoped>
.booking-page {
  min-height: 100vh;
  background-color: $bg-page;
  padding: $spacing-md;
}

.page-header {
  text-align: center;
  margin-bottom: $spacing-xl;

  .page-title {
    display: block;
    font-size: $font-2xl;
    font-weight: $weight-bold;
    color: $text-primary;
    margin-bottom: $spacing-sm;
  }

  .page-subtitle {
    display: block;
    font-size: $font-md;
    color: $text-secondary;
  }
}

.booking-form {
  background-color: $bg-card;
  border-radius: $border-radius-lg;
  padding: $spacing-lg;
  margin-bottom: $spacing-xl;
  box-shadow: $shadow-light;
}

.form-item {
  margin-bottom: $spacing-lg;

  &:last-child {
    margin-bottom: 0;
  }

  .form-label {
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: $font-md;
    font-weight: $weight-medium;
    color: $text-primary;
    margin-bottom: $spacing-sm;
  }

  .picker-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: $spacing-md;
    background-color: $bg-page;
    border-radius: $border-radius-md;
    border: 2rpx solid $border-light;

    &.disabled {
      background-color: #f5f5f5;
      opacity: 0.6;
    }

    .picker-text {
      font-size: $font-md;
      color: $text-primary;
    }
  }
}

.search-button-container {
  padding: 0 $spacing-md;

  .search-button {
    width: 100%;
    height: 88rpx;
    background-color: $primary-orange;
    color: $text-white;
    font-size: $font-lg;
    font-weight: $weight-medium;
    border-radius: $border-radius-round;
    border: none;
    display: flex;
    align-items: center;
    justify-content: center;

    &.disabled {
      background-color: #cccccc;
      color: #999999;
    }

    &:not(.disabled):active {
      background-color: #e68a21;
    }
  }
}
</style>
```

### 步骤3: 创建关联页面

**pages/vehicles/index.vue** - 车辆列表页面
```vue
<template>
  <view class="vehicles-page">
    <view class="search-header">
      <view class="search-info">
        <text class="search-title">可用房车</text>
        <text class="search-count">共 {{ vehicleList.length }} 辆</text>
      </view>
      <button class="filter-button" @click="showFilter = true">
        <uni-icons type="settings" size="20"></uni-icons>
        筛选
      </button>
    </view>

    <view class="vehicle-list">
      <VehicleCard
        v-for="vehicle in vehicleList"
        :key="vehicle.id"
        :vehicle="vehicle"
        @click="onVehicleClick"
      />
    </view>

    <!-- 空状态 -->
    <view v-if="vehicleList.length === 0" class="empty-state">
      <uni-icons type="info" size="60" color="#999"></uni-icons>
      <text class="empty-text">暂无可用房车</text>
      <text class="empty-subtitle">请调整查询条件重新搜索</text>
    </view>
  </view>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import VehicleCard from '@/components/business/vehicle/VehicleCard.vue'

const vehicleList = ref([])
const showFilter = ref(false)

const onVehicleClick = (vehicle: any) => {
  uni.navigateTo({
    url: `/pages/vehicles/detail?id=${vehicle.id}`
  })
}

onMounted(() => {
  // 获取页面参数
  const pages = getCurrentPages()
  const currentPage = pages[pages.length - 1]
  console.log('查询参数:', currentPage.options)

  // 加载车辆数据
  loadVehicles()
})

const loadVehicles = () => {
  // 模拟车辆数据
  vehicleList.value = [
    {
      id: 1,
      name: '豪华A型房车',
      price: 800,
      rating: 4.8,
      image: '/static/vehicle/default-vehicle.png',
      features: ['自动挡', '6座', '可住4人']
    },
    {
      id: 2,
      name: '舒适B型房车',
      price: 600,
      rating: 4.6,
      image: '/static/vehicle/default-vehicle.png',
      features: ['手动挡', '5座', '可住3人']
    }
  ]
}
</script>

<style lang="scss" scoped>
.vehicles-page {
  min-height: 100vh;
  background-color: $bg-page;
}

.search-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: $spacing-md;
  background-color: $bg-card;

  .search-info {
    .search-title {
      font-size: $font-xl;
      font-weight: $weight-bold;
      color: $text-primary;
    }

    .search-count {
      font-size: $font-sm;
      color: $text-secondary;
    }
  }

  .filter-button {
    display: flex;
    align-items: center;
    gap: $spacing-xs;
    padding: $spacing-sm $spacing-md;
    background-color: $bg-page;
    border-radius: $border-radius-md;
    font-size: $font-sm;
    color: $text-secondary;
  }
}

.vehicle-list {
  padding: $spacing-md;
}

.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx $spacing-md;

  .empty-text {
    font-size: $font-lg;
    color: $text-secondary;
    margin: $spacing-md 0 $spacing-sm;
  }

  .empty-subtitle {
    font-size: $font-sm;
    color: $text-tertiary;
  }
}
</style>
```

---

## 🧪 页面测试

### 1. 功能测试

打开 HBuilderX，运行到微信开发者工具：

```
步骤:
1. 右键 pages/booking/index.vue
2. 选择"运行到浏览器"
3. 或运行到微信开发者工具
4. 测试页面功能
```

### 2. 测试检查清单

- [ ] 页面正常显示
- [ ] 城市选择器工作正常
- [ ] 门店选择器联动正常
- [ ] 时间选择器工作正常
- [ ] 异地还车开关正常
- [ ] 表单验证正确
- [ ] 查询按钮状态正确
- [ ] 页面跳转正常
- [ ] 样式显示正确

---

## 🎨 样式优化

### 1. 优化交互体验

添加加载状态和动画效果：

```vue
<template>
  <!-- 在按钮上添加加载状态 -->
  <button
    class="search-button"
    :class="{ disabled: !canSearch, loading: isLoading }"
    :disabled="!canSearch || isLoading"
    @click="onSearch"
  >
    <uni-icons v-if="isLoading" type="spinner-cycle" size="20" color="white"></uni-icons>
    {{ isLoading ? '查询中...' : '查询可用房车' }}
  </button>
</template>

<script setup lang="ts">
const isLoading = ref(false)

const onSearch = async () => {
  if (!canSearch.value || isLoading.value) return

  isLoading.value = true

  try {
    // 模拟API调用
    await new Promise(resolve => setTimeout(resolve, 1000))

    // 跳转到车辆列表
    uni.navigateTo({
      url: '/pages/vehicles/index?' + new URLSearchParams(searchData as any).toString()
    })
  } catch (error) {
    uni.showToast({
      title: '查询失败，请重试',
      icon: 'none'
    })
  } finally {
    isLoading.value = false
  }
}
</script>

<style lang="scss" scoped>
.search-button {
  &.loading {
    opacity: 0.8;
  }
}
</style>
```

### 2. 添加过渡动画

```scss
.form-item {
  transition: all 0.3s ease;

  .picker-content {
    transition: all 0.2s ease;

    &:active {
      transform: scale(0.98);
    }
  }
}

.search-button {
  transition: all 0.3s ease;
}
```

---

## 🔍 常见问题解决

### 问题1: picker 组件不显示选项
```javascript
// 解决方案: 确保数据格式正确
const cityList = ref(['北京', '上海', '广州']) // ✅ 正确
const cityList = ref([{name: '北京'}, {name: '上海'}]) // ❌ 错误
```

### 问题2: 日期时间选择器报错
```javascript
// 解决方案: 使用正确的日期格式
pickupTime.value = '2025-11-24 10:00' // ✅ 正确格式
pickupTime.value = new Date().toISOString() // ❌ 需要转换
```

### 问题3: 页面跳转参数丢失
```javascript
// 解决方案: 正确编码URL参数
const params = new URLSearchParams(searchData as any).toString()
uni.navigateTo({
  url: `/pages/vehicles/index?${params}`
})
```

---

## ✅ 开发完成检查

### 功能完整性
- [ ] 所有表单字段功能正常
- [ ] 数据验证逻辑正确
- [ ] 异地还车功能完整
- [ ] 页面跳转参数传递正确
- [ ] 用户体验良好

### 代码质量
- [ ] TypeScript 类型定义完整
- [ ] 组件结构清晰
- [ ] 响应式数据使用正确
- [ ] 计算属性使用合理
- [ ] 代码注释充分

### 样式规范
- [ ] 使用统一样式变量
- [ ] 响应式单位使用 upx
- [ ] 交互状态反馈明显
- [ ] 布局美观合理
- [ ] 符合设计规范

---

## 🎯 下一步学习

页面开发完成后，建议继续学习：

1. **[组件开发指南](../04-组件开发/组件概览.md)** - 学习创建可复用组件
2. **[API接口开发](../05-API接口/接口开发指南.md)** - 学习与后端交互
3. **[状态管理深入](../05-页面开发/状态管理指南.md)** - 学习复杂状态管理
4. **[性能优化指南](../03-开发指南/性能优化指南.md)** - 学习页面性能优化

---

**文档维护**: 叨叨房车技术团队
**最后更新**: 2025-11-24
**版本**: v1.0.0
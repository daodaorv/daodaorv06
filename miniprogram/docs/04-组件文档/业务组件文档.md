# 叨叨房车小程序业务组件文档

**文档版本**: v1.0.0 | **创建时间**: 2025-11-24 | **维护团队**: 叨叨房车技术团队

> ⚠️ **重要提醒**: 业务组件专注特定业务场景，开发前请确认无法通过组合现有基础组件实现相同功能。

---

## 📋 业务组件概览

叨叨房车小程序共有 **10个** 业务组件，分别服务于房车租赁、订单管理、支付、众筹等核心业务场景。

### 组件分类统计

| 业务分类 | 组件数量 | 核心功能 | 开发状态 |
|----------|----------|----------|----------|
| **房车业务** | 4个 | 车辆展示、筛选、预订、骨架屏 | 🔄 需优化 |
| **订单业务** | 2个 | 订单汇总、状态展示 | ✅ 稳定 |
| **支付业务** | 1个 | 支付方式选择 | ✅ 稳定 |
| **众筹业务** | 1个 | 项目展示 | ✅ 稳定 |
| **服务业务** | 1个 | 服务列表展示 | ✅ 稳定 |
| **表单业务** | 1个 | 认证表单 | ✅ 稳定 |

---

## 🚗 房车业务组件

### 1. VehicleCard - 房车卡片组件

#### 组件说明
用于房车列表和详情页面的车辆信息展示，包含车辆图片、基本信息、价格、评分等。

**⚠️ 优化提醒**: 当前组件过于复杂（639行），需要重构拆分。

#### Props
| 属性名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| vehicle | Object | - | 房车信息对象（必须） |
| showStore | Boolean | true | 是否显示门店信息 |
| showDistance | Boolean | true | 是否显示距离 |
| favorite | Boolean | false | 是否已收藏 |
| compact | Boolean | false | 是否紧凑模式 |

#### Events
| 事件名 | 参数 | 说明 |
|--------|------|------|
| click | (vehicle: Object) | 点击卡片时触发 |
| favorite-change | (vehicle: Object, isFavorite: Boolean) | 收藏状态改变时触发 |
| store-click | (store: Object) | 点击门店时触发 |

#### 使用示例
```vue
<template>
  <VehicleCard
    :vehicle="vehicleData"
    :show-store="true"
    :show-distance="true"
    :favorite="isFavorite"
    @click="goToDetail"
    @favorite-change="handleFavoriteChange"
  />
</template>

<script setup>
const vehicleData = ref({
  id: 'V001',
  name: '奔驰依维柯C型房车',
  brand: '奔驰',
  type: 'C型',
  price: 800,
  rating: 4.8,
  reviews: 126,
  images: [
    'https://example.com/vehicle1.jpg',
    'https://example.com/vehicle2.jpg'
  ],
  features: ['自动挡', '6座', '可住4人'],
  store: {
    id: 'S001',
    name: '北京朝阳店',
    distance: 5.2,
    address: '朝阳区xxx路xxx号'
  }
})
</script>
```

#### 重构计划
当前VehicleCard组件过于复杂，计划拆分为：
- `VehicleImage` - 车辆图片展示
- `VehicleInfo` - 车辆基本信息
- `VehicleFooter` - 底部操作区域

### 2. VehicleFilter - 房车筛选器

#### 组件说明
用于房车列表页面的筛选条件选择，支持品牌、车型、价格区间等多维度筛选。

#### Props
| 属性名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| brands | Array | [] | 品牌选项列表 |
| types | Array | [] | 车型选项列表 |
| priceRange | Array | [0, 5000] | 价格区间范围 |
| filters | Object | - | 当前筛选条件 |
| showDistance | Boolean | true | 是否显示距离筛选 |

#### Events
| 事件名 | 参数 | 说明 |
|--------|------|------|
| filter-change | (filters: Object) | 筛选条件改变时触发 |
| reset-click | () | 点击重置按钮时触发 |

#### 使用示例
```vue
<template>
  <VehicleFilter
    :brands="brandOptions"
    :types="typeOptions"
    :price-range="[0, 2000]"
    :filters="currentFilters"
    @filter-change="handleFilterChange"
  />
</template>

<script setup>
const brandOptions = ref([
  { id: 'benz', name: '奔驰', count: 23 },
  { id: 'ford', name: '福特', count: 18 },
  { id: 'iveco', name: '依维柯', count: 15 }
])

const typeOptions = ref([
  { id: 'A', name: 'A型', count: 20 },
  { id: 'B', name: 'B型', count: 25 },
  { id: 'C', name: 'C型', count: 11 }
])

const currentFilters = ref({
  brand: '',
  type: '',
  priceRange: [0, 2000],
  distance: 10
})
</script>
```

### 3. VehicleBooking - 房车预订表单

#### 组件说明
用于首页和预订页面的房车预订信息填写，包含取还车时间、地点选择等。

#### Props
| 属性名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| bookingData | Object | - | 预订数据对象 |
| stores | Array | [] | 门店列表 |
| showReturnStore | Boolean | false | 是否显示还车门店选择 |
| disabled | Boolean | false | 是否禁用表单 |

#### Events
| 事件名 | 参数 | 说明 |
|--------|------|------|
| booking-change | (bookingData: Object) | 预订信息改变时触发 |
| submit-click | (bookingData: Object) | 点击查询按钮时触发 |

#### 使用示例
```vue
<template>
  <VehicleBooking
    :booking-data="bookingInfo"
    :stores="storeList"
    :show-return-store="enableReturnStore"
    @booking-change="handleBookingChange"
    @submit-click="handleSubmit"
  />
</template>

<script setup>
const bookingInfo = ref({
  pickupCity: '',
  pickupStore: '',
  pickupDate: '',
  pickupTime: '10:00',
  returnDate: '',
  returnTime: '18:00',
  returnToDifferentStore: false,
  returnStore: ''
})
</script>
```

### 4. VehicleSkeleton - 房车骨架屏

#### 组件说明
用于房车列表加载时的占位显示，提升用户体验。

#### Props
| 属性名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| rows | Number | 3 | 显示行数 |
| animate | Boolean | true | 是否显示动画 |
| height | String/Number | 200 | 骨架屏高度（rpx） |

#### 使用示例
```vue
<template>
  <view v-if="loading" class="skeleton-container">
    <VehicleSkeleton
      v-for="i in 3"
      :key="i"
      :rows="1"
      :animate="true"
      :height="200"
    />
  </view>
  <view v-else>
    <!-- 实际内容 -->
  </view>
</template>
```

---

## 📋 订单业务组件

### 1. OrderSummary - 订单汇总组件

#### 组件说明
用于订单确认和支付页面的费用明细展示，包含租金、保险、服务费等。

#### Props
| 属性名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| order | Object | - | 订单信息对象 |
| showCoupon | Boolean | true | 是否显示优惠券选择 |
| editable | Boolean | true | 是否可编辑服务选项 |

#### Events
| 事件名 | 参数 | 说明 |
|--------|------|------|
| coupon-change | (coupon: Object) | 优惠券改变时触发 |
| service-change | (services: Array) | 服务改变时触发 |

#### 使用示例
```vue
<template>
  <OrderSummary
    :order="orderData"
    :show-coupon="true"
    :editable="true"
    @coupon-change="handleCouponChange"
  />
</template>

<script setup>
const orderData = ref({
  vehicle: {
    name: '奔驰依维柯C型房车',
    dailyPrice: 800
  },
  rental: {
    pickupDate: '2024-12-01',
    returnDate: '2024-12-03',
    days: 2
  },
  insurance: {
    type: '标准险',
    price: 120
  },
  services: [
    { name: '儿童座椅', price: 50 }
  ],
  coupon: null,
  totalAmount: 1770
})
</script>
```

### 2. OrderStatus - 订单状态组件

#### 组件说明
用于订单列表和详情页面的订单状态展示，包含状态图标、进度条等。

#### Props
| 属性名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| status | String | - | 订单状态码 |
| steps | Array | [] | 订单步骤列表 |
| showActions | Boolean | true | 是否显示操作按钮 |

#### Events
| 事件名 | 参数 | 说明 |
|--------|------|------|
| action-click | (action: String, order: Object) | 操作按钮点击时触发 |

#### 使用示例
```vue
<template>
  <OrderStatus
    :status="orderStatus"
    :steps="orderSteps"
    :show-actions="true"
    @action-click="handleAction"
  />
</template>

<script setup>
const orderStatus = ref('CONFIRMED')
const orderSteps = ref([
  { key: 'CREATED', title: '订单创建', completed: true },
  { key: 'CONFIRMED', title: '已确认', completed: true },
  { key: 'PICKUP', title: '待取车', completed: false },
  { key: 'USING', title: '使用中', completed: false },
  { key: 'RETURNED', title: '已完成', completed: false }
])
</script>
```

---

## 💳 支付业务组件

### 1. PaymentMethod - 支付方式组件

#### 组件说明
用于支付页面的支付方式选择，支持微信支付、支付宝、余额支付等。

#### Props
| 属性名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| methods | Array | [] | 支付方式列表 |
| selected | String | - | 已选择的支付方式 |
| balance | Number | 0 | 账户余额 |

#### Events
| 事件名 | 参数 | 说明 |
|--------|------|------|
| method-change | (method: String) | 支付方式改变时触发 |
| balance-recharge | () | 点击余额充值时触发 |

#### 使用示例
```vue
<template>
  <PaymentMethod
    :methods="paymentMethods"
    :selected="selectedMethod"
    :balance="userBalance"
    @method-change="handleMethodChange"
  />
</template>

<script setup>
const paymentMethods = ref([
  { id: 'wechat', name: '微信支付', icon: 'weixin', available: true },
  { id: 'alipay', name: '支付宝', icon: 'zhifubao', available: true },
  { id: 'balance', name: '余额支付', icon: 'wallet', available: true }
])

const selectedMethod = ref('wechat')
const userBalance = ref(580.50)
</script>
```

---

## 💰 众筹业务组件

### 1. ProjectCard - 众筹项目卡片

#### 组件说明
用于众筹项目列表和详情页面的项目信息展示。

#### Props
| 属性名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| project | Object | - | 项目信息对象 |
| showProgress | Boolean | true | 是否显示进度条 |
| showActions | Boolean | true | 是否显示操作按钮 |

#### Events
| 事件名 | 参数 | 说明 |
|--------|------|------|
| project-click | (project: Object) | 点击项目时触发 |
| support-click | (project: Object) | 点击支持按钮时触发 |

#### 使用示例
```vue
<template>
  <ProjectCard
    :project="projectData"
    :show-progress="true"
    :show-actions="true"
    @project-click="goToProjectDetail"
    @support-click="handleSupport"
  />
</template>

<script setup>
const projectData = ref({
  id: 'P001',
  name: '豪华房车众筹项目',
  description: '投资高端房车，享受稳定收益',
  targetAmount: 1000000,
  currentAmount: 680000,
  annualReturn: '8-12%',
  period: 24,
  riskLevel: '中低',
  image: 'https://example.com/project.jpg'
})
</script>
```

---

## 🛎️ 服务业务组件

### 1. ServiceList - 服务列表组件

#### 组件说明
用于相关服务的展示和选择，采用网格布局。

#### Props
| 属性名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| services | Array | [] | 服务列表 |
| columns | Number | 2 | 每行显示数量 |
| selectable | Boolean | false | 是否可选择 |

#### Events
| 事件名 | 参数 | 说明 |
|--------|------|------|
| service-click | (service: Object, index: Number) | 点击服务时触发 |

#### 使用示例
```vue
<template>
  <ServiceList
    :services="serviceList"
    :columns="2"
    :selectable="true"
    @service-click="handleServiceClick"
  />
</template>

<script setup>
const serviceList = ref([
  {
    id: 'S001',
    name: '机场接送',
    price: 100,
    description: '机场到门店接送服务',
    icon: 'plane'
  },
  {
    id: 'S002',
    name: '儿童座椅',
    price: 50,
    description: '儿童安全座椅租赁',
    icon: 'baby-car-seat'
  }
])
</script>
```

---

## 📝 表单业务组件

### 1. AuthForm - 认证表单组件

#### 组件说明
用于用户登录、注册、实名认证等表单场景。

#### Props
| 属性名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| type | String | 'login' | 表单类型：login/register/auth |
| fields | Array | [] | 表单字段配置 |
| loading | Boolean | false | 是否提交中 |

#### Events
| 事件名 | 参数 | 说明 |
|--------|------|------|
| submit | (formData: Object) | 表单提交时触发 |
| field-change | (field: String, value: any) | 字段值改变时触发 |

#### 使用示例
```vue
<template>
  <AuthForm
    :type="'login'"
    :fields="loginFields"
    :loading="submitting"
    @submit="handleLogin"
  />
</template>

<script setup>
const loginFields = ref([
  {
    name: 'phone',
    label: '手机号',
    type: 'number',
    required: true,
    placeholder: '请输入手机号'
  },
  {
    name: 'password',
    label: '密码',
    type: 'password',
    required: true,
    placeholder: '请输入密码'
  }
])

const submitting = ref(false)
</script>
```

---

## 🔧 业务组件开发规范

### 开发原则

1. **业务单一性**: 每个组件专注一个特定业务场景
2. **数据驱动**: 通过props传入数据，通过events通知变化
3. **可复用性**: 设计通用的接口，支持不同业务场景
4. **性能优先**: 考虑大数据量场景，使用虚拟滚动等技术
5. **错误处理**: 完善的错误边界和异常处理

### 组件结构模板

```vue
<template>
  <view class="business-component">
    <!-- 组件模板内容 -->
  </view>
</template>

<script setup lang="ts">
import { computed, ref } from 'vue'

// 接口定义
interface ComponentProps {
  // props定义
}

interface ComponentEmits {
  // events定义
}

// Props定义
const props = withDefaults(defineProps<ComponentProps>(), {
  // 默认值
})

// Events定义
const emit = defineEmits<ComponentEmits>()

// 响应式数据
const internalState = ref()

// 计算属性
const computedValue = computed(() => {
  // 计算逻辑
  return props.data
})

// 方法定义
const handleClick = () => {
  emit('click', computedValue.value)
}
</script>

<style scoped lang="scss">
.business-component {
  // 组件样式
}
</style>
```

---

## 📖 相关文档

- **[组件总览](./组件总览.md)** - 组件体系概览
- **[基础组件文档](./基础组件文档.md)** - 基础组件详细说明
- **[组件使用示例](./组件使用示例.md)** - 实际使用场景
- **[组件开发规范](../03-开发指南/组件开发规范.md)** - 开发标准和规范
- **[uni-app开发指导](../03-开发指南/uni-app开发指导文档.md)** - 框架使用指南

---

## 🚨 组件优化计划

### 🔥 立即执行（本周）
- [ ] 重构VehicleCard组件，拆分为3个子组件
- [ ] 优化VehicleBooking性能，减少不必要的重渲染
- [ ] 完善组件的错误处理和边界情况

### ⚡ 短期计划（1个月）
- [ ] 建立组件单元测试覆盖
- [ ] 完善组件TypeScript类型定义
- [ ] 建立组件性能监控机制

### 📚 长期规划（2个月）
- [ ] 建立组件Storybook文档
- [ ] 实现组件自动化测试
- [ ] 建立组件版本管理机制

---

**文档维护**: 叨叨房车技术团队
**最后更新**: 2025-11-24
**文档版本**: v1.0.0
**下次审核**: 2025-12-24
# 小程序端代码重复分析报告

**项目**: 叨叨房车租赁小程序 (miniprogram)
**分析日期**: 2025-12-21
**分析范围**: 全量代码重复性分析

---

## 一、分析概览

### 1.1 项目规模

| 指标 | 数量 |
|------|------|
| 页面总数 | 62 |
| 组件总数 | 15 |
| 工具函数文件 | 7 |
| 总代码行数 | ~39,000 |

### 1.2 重复代码统计

| 重复类型 | 涉及文件数 | 重复次数 | 估计重复代码行数 |
|---------|----------|---------|----------------|
| 功能逻辑重复 | 57 | 137 | ~2,000+ |
| 业务组件重复 | 60 | 381 | ~1,500+ |
| 布局结构重复 | 20 | 75 | ~800+ |
| 工具函数重复 | 23 | 35 | ~300+ |
| **总计** | **62** | **628** | **~4,600+** |

**代码重复率**: 约 **12%** (4,600 / 39,000)

---

## 二、页面级别重复代码分析

### 2.1 重复的布局结构

#### 2.1.1 底部操作栏 (20个页面)

**涉及页面**:
- `pages/order/list.vue`
- `pages/order/confirm.vue`
- `pages/order/pay.vue`
- `pages/order/detail.vue`
- `pages/campsite/booking.vue`
- `pages/tour/booking.vue`
- `pages/membership/purchase.vue`
- `pages/rating/create.vue`
- 等共 20 个页面

**重复代码示例**:
```scss
.bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 32rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  background: linear-gradient(180deg, rgba(255,255,255,0.95) 0%, rgba(255,255,255,1) 100%);
  backdrop-filter: blur(20rpx);
  box-shadow: 0 -8rpx 24rpx rgba(0,0,0,0.08);
  z-index: 100;
}
```

**重复代码行数**: ~50行/页面 × 20 = **~1,000行**

#### 2.1.2 空状态展示 (12个页面)

**涉及页面**:
- `pages/vehicle/list.vue`
- `pages/tour/list.vue`
- `pages/campsite/list.vue`
- `pages/profile/favorites.vue`
- `pages/profile/coupons.vue`
- `pages/profile/points.vue`
- 等共 12 个页面

**重复代码示例**:
```vue
<view v-if="isEmpty" class="empty-state">
  <u-icon name="file-text" size="80" color="#CCCCCC"></u-icon>
  <text class="empty-text">暂无数据</text>
</view>
```

**重复代码行数**: ~10行/页面 × 12 = **~120行**

#### 2.1.3 加载状态展示 (12个页面)

**涉及页面**: 与空状态展示相同

**重复代码示例**:
```typescript
const loading = ref(false);
const refreshing = ref(false);

const loadData = async () => {
  if (loading.value) return;
  loading.value = true;
  try {
    // 加载数据
  } finally {
    loading.value = false;
  }
};
```

**重复代码行数**: ~15行/页面 × 12 = **~180行**

---

### 2.2 重复的业务组件

#### 2.2.1 车辆卡片 (12个页面)

**涉及页面**:
- `pages/vehicle/list.vue`
- `pages/vehicle/detail.vue`
- `pages/order/detail.vue`
- `pages/order/confirm.vue`
- `pages/special-offer/list.vue`
- `pages/profile/favorites.vue`
- 等共 12 个页面

**重复模式**:
| 模式 | 重复次数 |
|------|---------|
| 车辆图片 + 名称 + 规格 | 12 |
| 价格展示（¥ + 数字 + 单位）| 12 |
| 评分展示（星级 + 分数）| 8 |

**重复代码行数**: ~150-200行

#### 2.2.2 订单卡片 (10个页面)

**涉及页面**:
- `pages/order/list.vue`
- `pages/order/detail.vue`
- `pages/order/confirm.vue`
- `pages/profile/index.vue`
- `pages/rating/create.vue`
- 等共 10 个页面

**重复模式**:
| 模式 | 重复次数 |
|------|---------|
| 订单头部（名称 + 状态）| 10 |
| 时间信息（取车/还车时间）| 10 |
| 金额显示 | 10 |
| 状态颜色映射 | 8 |

**重复代码行数**: ~120-180行

#### 2.2.3 筛选组件 (21个页面)

**涉及页面**:
- `pages/vehicle/list.vue`
- `pages/campsite/list.vue`
- `pages/tour/list.vue`
- `pages/special-offer/list.vue`
- `pages/coupon-mall/index.vue`
- 等共 21 个页面

**重复模式**:
| 模式 | 重复次数 |
|------|---------|
| 筛选栏UI结构 | 21 |
| 筛选弹窗结构 | 18 |
| 筛选逻辑函数 | 15 |

**重复代码行数**: ~200-300行

#### 2.2.4 日期选择器 (24个页面)

**涉及页面**: 几乎所有需要日期输入的页面

**重复模式**:
| 模式 | 重复次数 |
|------|---------|
| 日期选择器UI | 24 |
| 时间选择器UI | 12 |
| 日期格式化函数 | 18 |

**重复代码行数**: ~180-250行

#### 2.2.5 价格展示 (35个页面)

**涉及页面**: 所有涉及金额的页面

**重复模式**:
```vue
<view class="price-box">
  <text class="currency">¥</text>
  <text class="price">{{ price }}</text>
  <text class="unit">/日均</text>
</view>
```

**重复代码行数**: ~150-200行

---

### 2.3 重复的功能逻辑

#### 2.3.1 登录检查逻辑 (12个页面)

**涉及页面**:
- `pages/campsite/booking.vue`
- `pages/tour/booking.vue`
- `pages/order/confirm.vue`
- `pages/order/pay.vue`
- `pages/membership/purchase.vue`
- `pages/profile/address.vue`
- `pages/profile/settings.vue`
- `pages/profile/contacts.vue`
- `pages/community/publish.vue`
- `pages/profile/promotion-center.vue`
- `pages/profile/address-edit.vue`
- `pages/profile/contact-edit.vue`

**重复代码示例**:
```typescript
const ensureAuth = (options: any) => {
  redirectUrl.value = buildRedirectUrl('/pages/xxx', options || {});
  if (isLoggedIn()) {
    return true;
  }
  return requireLogin(redirectUrl.value);
};

onLoad((options: any) => {
  cachedRouteParams = options || {};
  pageReady.value = false;
  if (!ensureAuth(cachedRouteParams)) {
    return;
  }
  setupPage(cachedRouteParams);
});
```

**重复代码行数**: ~50-80行/页面 × 12 = **~600-960行**

#### 2.3.2 数据加载逻辑 (35个页面)

**涉及页面**: 几乎所有需要加载数据的页面

**重复代码示例**:
```typescript
const loadData = async () => {
  try {
    uni.showLoading({ title: '加载中...' });
    // Mock数据或API调用
    uni.hideLoading();
  } catch (error) {
    logger.error('加载失败:', error);
    uni.hideLoading();
  }
};
```

**重复代码行数**: ~30-100行/页面 × 35 = **~1,050-3,500行**

#### 2.3.3 分页加载逻辑 (13个页面)

**涉及页面**:
- `pages/tour/list.vue`
- `pages/special-offer/list.vue`
- `pages/profile/favorites.vue`
- `pages/campsite/list.vue`
- `pages/share/invite-records.vue`
- `pages/help/article.vue`
- `pages/rating/list.vue`
- `pages/hosting/model-list/index.vue`
- `pages/coupon-mall/index.vue`
- `pages/profile/points.vue`
- `pages/order/list.vue`
- 等共 13 个页面

**重复代码示例**:
```typescript
const page = ref(1);
const pageSize = ref(10);
const hasMore = ref(true);

const loadMore = async () => {
  if (!hasMore.value) return;
  page.value++;
  try {
    // 加载更多数据
  } catch (error) {
    logger.error('加载失败:', error);
  }
};

onReachBottom(() => {
  loadMore();
});
```

**重复代码行数**: ~40-60行/页面 × 13 = **~520-780行**

#### 2.3.4 图片上传逻辑 (7个页面)

**涉及页面**:
- `pages/hosting/old-car/index.vue`
- `pages/rating/create.vue`
- `pages/profile/complete-info.vue`
- `pages/community/publish.vue`
- `pages/profile/edit.vue`
- `pages/order/report.vue`
- `pages/order/confirm.vue`

**重复代码示例**:
```typescript
const handleChooseImage = () => {
  uni.chooseImage({
    count: 1,
    sizeType: ['compressed'],
    sourceType: ['camera', 'album'],
    success: async (res) => {
      const path = res.tempFilePaths[0];
      await uploadImage(path);
    }
  });
};

const uploadImage = async (localPath: string) => {
  uni.showLoading({ title: '上传中...' });
  try {
    const url = await uploadApi(localPath);
    uni.showToast({ title: '上传成功', icon: 'success' });
  } catch (error) {
    uni.showToast({ title: '上传失败', icon: 'none' });
  } finally {
    uni.hideLoading();
  }
};
```

**重复代码行数**: ~60-80行/页面 × 7 = **~420-560行**

#### 2.3.5 支付流程逻辑 (8个页面)

**涉及页面**:
- `pages/membership/purchase.vue`
- `pages/campsite/booking.vue`
- `pages/tour/booking.vue`
- `pages/profile/wallet.vue`
- `pages/order/pay.vue`
- `pages/order/list.vue`
- `pages/order/detail.vue`
- `pages/order/confirm.vue`

**重复代码行数**: ~100-150行/页面 × 8 = **~800-1,200行**

#### 2.3.6 Toast/Modal 提示 (57个页面)

**涉及页面**: 几乎所有页面

**重复模式**:
```typescript
uni.showToast({ title: '操作成功', icon: 'success' });
uni.showToast({ title: '操作失败', icon: 'none' });
uni.showModal({
  title: '提示',
  content: '确认删除吗？',
  success: (res) => { if (res.confirm) { /* ... */ } }
});
```

**重复代码行数**: ~5-20行/页面 × 57 = **~285-1,140行**

---

## 三、组件级别重复代码分析

### 3.1 组件代码量统计

| 组件 | 代码行数 | 状态 |
|------|---------|------|
| BookingForm.vue | 784 | ⚠️ 需要拆分 |
| RentDatePicker.vue | 580 | ⚠️ 需要优化 |
| InviteSheet.vue | 368 | 可接受 |
| CityStorePicker.vue | 263 | 良好 |
| VehicleCard.vue | 260 | 良好 |
| PosterPreview.vue | 254 | 良好 |
| VehicleFilter.vue | 213 | 良好 |
| ConfirmDialog.vue | 133 | 优秀 |
| ShareSheet.vue | 122 | 优秀 |
| NoticeBanner.vue | 95 | 优秀 |
| ErrorBoundary.vue | 93 | 优秀 |
| ServiceGrid.vue | 83 | 优秀 |
| AnnouncementBar.vue | 80 | 优秀 |
| EmptyState.vue | 74 | 优秀 |
| ErrorState.vue | 70 | 优秀 |

### 3.2 功能相似的组件

| 组件对 | 相似度 | 建议 |
|--------|--------|------|
| EmptyState + ErrorState | 90% | 合并为 StateDisplay |
| NoticeBanner + AnnouncementBar | 85% | 合并为 NoticeBar |

### 3.3 组件内重复逻辑

| 重复逻辑 | 涉及组件 | 建议 |
|---------|---------|------|
| 日期格式化 | BookingForm, RentDatePicker | 提取到 utils/format.ts |
| 弹窗控制 | ConfirmDialog, CityStorePicker, RentDatePicker | 提取到 composables/usePopup.ts |
| 城市门店选择 | BookingForm, CityStorePicker | 提取到 composables/useLocationPicker.ts |

---

## 四、工具函数重复分析

### 4.1 页面内重复定义的函数

| 函数名 | 重复次数 | 优先级 |
|--------|---------|--------|
| formatDate | 23 | 🔴 高 |
| getStatusText | 5 | 🔴 高 |
| getStatusColor | 2 | 🟡 中 |
| formatTime | 2 | 🟡 中 |
| maskUsername | 1 | 🟢 低 |
| calculateDistance | 2 | 🟡 中 |

### 4.2 缺失的工具函数

| 函数类型 | 建议文件 | 函数列表 |
|---------|---------|---------|
| 格式化 | format.ts | formatPrice, formatAmount, formatDate, formatTime, formatPhone |
| 验证 | validate.ts | validatePhone, validateEmail, validateIdCard, validatePassword |
| 状态映射 | statusMap.ts | getOrderStatusText, getOrderStatusColor, getTourStatusText |
| 时间计算 | time.ts | calculateDays, getTimeAgo, isToday |
| 数据处理 | data.ts | maskUsername, maskPhone, truncateText |

---

## 五、重复代码汇总

### 5.1 按类型统计

| 类型 | 重复次数 | 估计代码行数 | 占比 |
|------|---------|-------------|------|
| 登录检查逻辑 | 12 | ~800 | 17% |
| 数据加载逻辑 | 35 | ~2,000 | 43% |
| 分页加载逻辑 | 13 | ~650 | 14% |
| 图片上传逻辑 | 7 | ~500 | 11% |
| 支付流程逻辑 | 8 | ~1,000 | 22% |
| 布局结构重复 | 20 | ~1,300 | 28% |
| 工具函数重复 | 23 | ~300 | 7% |

### 5.2 按优先级统计

| 优先级 | 问题数 | 影响范围 | 建议处理时间 |
|--------|-------|---------|-------------|
| 🔴 高 | 5 | 35+ 页面 | 立即处理 |
| 🟡 中 | 8 | 15-30 页面 | 1周内处理 |
| 🟢 低 | 10 | <15 页面 | 后续优化 |

---

## 六、优化建议

### 6.1 立即处理（高优先级）

#### 1. 创建 `utils/format.ts`
```typescript
// 统一日期格式化（解决23次重复）
export const formatDate = (dateStr: string, format = 'MM月DD日 HH:mm') => {
  return dayjs(dateStr).format(format);
};

export const formatTime = (dateStr: string) => {
  return dayjs(dateStr).format('HH:mm');
};

export const formatPrice = (price: number) => {
  return `¥${price.toFixed(2)}`;
};
```

#### 2. 创建 `utils/statusMap.ts`
```typescript
// 统一状态映射（解决7次重复）
export const ORDER_STATUS_MAP = {
  pending_payment: { text: '待支付', color: '#FF4D4F' },
  pending_pickup: { text: '待取车', color: '#FF9F29' },
  renting: { text: '租赁中', color: '#00B578' },
  completed: { text: '已完成', color: '#2196F3' },
  cancelled: { text: '已取消', color: '#999999' }
};

export const getOrderStatusText = (status: string) => ORDER_STATUS_MAP[status]?.text || status;
export const getOrderStatusColor = (status: string) => ORDER_STATUS_MAP[status]?.color || '#999999';
```

#### 3. 创建 `composables/useListPagination.ts`
```typescript
// 统一分页逻辑（解决13次重复）
export function useListPagination<T>(fetchApi: Function) {
  const list = ref<T[]>([]);
  const page = ref(1);
  const hasMore = ref(true);
  const loading = ref(false);

  const loadMore = async () => { /* ... */ };
  const refresh = async () => { /* ... */ };

  return { list, page, hasMore, loading, loadMore, refresh };
}
```

#### 4. 创建 `composables/useImageUpload.ts`
```typescript
// 统一图片上传（解决7次重复）
export function useImageUpload(uploadApi: Function) {
  const uploading = ref(false);
  const imageUrl = ref('');

  const chooseAndUpload = async () => { /* ... */ };

  return { uploading, imageUrl, chooseAndUpload };
}
```

### 6.2 短期处理（中优先级）

1. **合并相似组件**
   - EmptyState + ErrorState → StateDisplay
   - NoticeBanner + AnnouncementBar → NoticeBar

2. **拆分大型组件**
   - BookingForm.vue (784行) → 拆分为多个子组件

3. **创建通用布局组件**
   - BottomBar.vue - 底部操作栏
   - FilterBar.vue - 筛选栏
   - ListCard.vue - 列表卡片

### 6.3 长期优化（低优先级）

1. 建立组件库文档
2. 添加单元测试
3. 性能优化（虚拟列表等）

---

## 七、预期收益

### 7.1 代码量减少

| 优化项 | 预计减少代码行数 |
|--------|----------------|
| 提取工具函数 | ~500 行 |
| 提取 Composables | ~1,500 行 |
| 合并相似组件 | ~200 行 |
| 创建通用布局组件 | ~800 行 |
| **总计** | **~3,000 行** |

### 7.2 维护性提升

- 修改一处逻辑，全局生效
- 减少 bug 出现概率
- 提高开发效率

### 7.3 代码重复率变化

- **优化前**: ~12% (4,600 / 39,000)
- **优化后**: ~4% (1,600 / 39,000)
- **降低**: **8个百分点**

---

## 八、总结

本次分析发现小程序项目存在较严重的代码重复问题，主要集中在：

1. **功能逻辑重复** - 登录检查、数据加载、分页加载等在多个页面重复实现
2. **业务组件重复** - 车辆卡片、订单卡片、筛选组件等在多个页面重复定义
3. **工具函数重复** - formatDate 函数在 23 个页面重复定义
4. **布局结构重复** - 底部操作栏、空状态展示等在多个页面重复

通过提取公共 Composables、工具函数和组件，预计可以减少约 3,000 行重复代码，将代码重复率从 12% 降低到 4%。

---

**报告生成时间**: 2025-12-21
**分析工具**: Claude Code

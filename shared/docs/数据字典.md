# 叨叨房车数据字典（统一规范）

**文档版本**: v1.0.0 | **创建时间**: 2026-01-09 | **规范**: 全栈统一驼峰命名

---

## 📋 命名规范

### 统一使用驼峰命名（camelCase）

| 层级 | 命名规范 | 示例 |
|------|----------|------|
| **前端** | camelCase | `userId`, `userName`, `createdAt` |
| **后端** | camelCase | `userId`, `userName`, `createdAt` |
| **数据库** | snake_case | `user_id`, `user_name`, `created_at` |
| **转换层** | ORM自动转换 | Sequelize `underscored: true` |

---

## 🔑 核心枚举定义

### 1. 订单状态（OrderStatus）

| 枚举值 | 显示文本 | 说明 | 前端使用 | 后端使用 |
|--------|----------|------|----------|----------|
| `pending_payment` | 待支付 | 订单已创建，等待支付 | ✅ | ✅ |
| `pending_confirmation` | 待确认 | 已支付，等待门店确认 | ✅ | ✅ |
| `pending_pickup` | 待取车 | 已确认，等待取车 | ✅ | ✅ |
| `in_progress` | 租赁中 | 已取车，正在使用 | ✅ | ✅ |
| `pending_return` | 待还车 | 使用结束，等待还车 | ✅ | ✅ |
| `completed` | 已完成 | 订单已完成 | ✅ | ✅ |
| `cancelled` | 已取消 | 订单已取消 | ✅ | ✅ |

**TypeScript定义**:
```typescript
export enum OrderStatus {
  PENDING_PAYMENT = 'pending_payment',
  PENDING_CONFIRMATION = 'pending_confirmation',
  PENDING_PICKUP = 'pending_pickup',
  IN_PROGRESS = 'in_progress',
  PENDING_RETURN = 'pending_return',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled'
}
```

---

### 2. 车辆状态（VehicleStatus）

| 枚举值 | 显示文本 | 说明 |
|--------|----------|------|
| `available` | 可用 | 车辆可以出租 |
| `rented` | 已租出 | 车辆正在租赁中 |
| `maintenance` | 维护中 | 车辆正在维修保养 |
| `reserved` | 已预订 | 车辆已被预订 |
| `offline` | 下线 | 车辆暂时不可用 |

---

### 3. 支付状态（PaymentStatus）

| 枚举值 | 显示文本 | 说明 |
|--------|----------|------|
| `pending` | 待支付 | 等待用户支付 |
| `paid` | 已支付 | 支付成功 |
| `failed` | 支付失败 | 支付失败 |
| `refunding` | 退款中 | 正在退款 |
| `refunded` | 已退款 | 退款成功 |

---

### 4. 用户角色（UserRole）

| 枚举值 | 显示文本 | 说明 |
|--------|----------|------|
| `customer` | 普通用户 | 小程序注册用户 |
| `hosting_owner` | 托管车主 | 托管车辆的车主 |
| `plus_member` | PLUS会员 | 付费会员用户 |

---

### 5. 员工角色（EmployeeRole）

| 枚举值 | 显示文本 | 数据权限 |
|--------|----------|----------|
| `platform_admin` | 平台管理员 | 全平台数据 |
| `regional_manager` | 区域经理 | 所辖区域数据 |
| `store_manager` | 门店经理 | 本门店数据 |
| `store_staff` | 门店员工 | 分配任务数据 |

---

## 📊 核心数据模型

### 用户模型（User）

```typescript
interface User {
  userId: number              // 用户ID
  userName: string            // 用户名
  realName?: string           // 真实姓名
  phoneNumber: string         // 手机号
  email?: string              // 邮箱
  avatar?: string             // 头像URL
  gender?: 'male' | 'female'  // 性别
  birthday?: string           // 生日（YYYY-MM-DD）
  idCard?: string             // 身份证号
  driverLicense?: string      // 驾驶证号
  status: 'active' | 'inactive' | 'blocked'  // 状态
  tags?: number[]             // 用户标签ID数组
  isPlusMember: boolean       // 是否PLUS会员
  plusExpireAt?: string       // 会员到期时间
  createdAt: string           // 创建时间
  updatedAt: string           // 更新时间
}
```

---

### 订单模型（Order）

```typescript
interface Order {
  orderId: number             // 订单ID
  orderNo: string             // 订单号
  productType: 'vehicle' | 'campsite' | 'special_offer' | 'tour'  // 产品类型
  userId: number              // 用户ID
  vehicleId?: number          // 车辆ID
  storeId: number             // 门店ID
  startDate: string           // 开始日期
  endDate: string             // 结束日期
  days: number                // 租赁天数
  totalAmount: number         // 订单总金额
  paidAmount: number          // 已支付金额
  orderStatus: OrderStatus    // 订单状态
  paymentStatus: PaymentStatus // 支付状态
  createdAt: string           // 创建时间
  updatedAt: string           // 更新时间
}
```

---

### 车辆模型（Vehicle）

```typescript
interface Vehicle {
  vehicleId: number           // 车辆ID
  vehicleNo: string           // 车牌号
  modelId: number             // 车型ID
  storeId: number             // 所属门店ID
  vehicleStatus: VehicleStatus // 车辆状态
  sourceType: 'owned' | 'hosting' | 'cooperation' | 'crowdfunding'  // 来源类型
  dailyPrice: number          // 日租金
  images: string[]            // 车辆图片
  mileage: number             // 里程数
  purchaseDate: string        // 购买日期
  createdAt: string           // 创建时间
  updatedAt: string           // 更新时间
}
```

---

## 🔄 数据类型规范

### 日期时间格式

| 类型 | 格式 | 示例 | 说明 |
|------|------|------|------|
| **日期** | `YYYY-MM-DD` | `2026-01-09` | 日期选择器 |
| **时间戳** | `YYYY-MM-DD HH:mm:ss` | `2026-01-09 14:30:00` | 创建/更新时间 |
| **ISO 8601** | `YYYY-MM-DDTHH:mm:ss.sssZ` | `2026-01-09T14:30:00.000Z` | API传输 |

### 金额格式

| 类型 | 精度 | 单位 | 示例 |
|------|------|------|------|
| **金额** | 2位小数 | 元 | `1999.99` |
| **分润比例** | 2位小数 | 百分比 | `30.00` (表示30%) |

### 布尔值

| 类型 | 值 | 说明 |
|------|------|------|
| **布尔** | `true` / `false` | 统一使用布尔值，不使用 0/1 |

---

**文档维护**: 叨叨房车技术团队
**最后更新**: 2026-01-09
